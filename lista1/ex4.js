// Criamos uma variável que começa como uma string vazia
let linha = "";

// O laço vai rodar 7 vezes
for (let i = 1; i <= 7; i++) {
  linha += "#"; // Adiciona um # ao final da string atual
  console.log(linha); // Exibe a linha atual no console
}